package com.venticas.dataTest;
	import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.test.context.SpringBootTest;
import org.springframework.test.context.junit4.SpringRunner;

import com.venticas.data.ShoppingCartData;

	@RunWith(SpringRunner.class)
	@SpringBootTest
	public class ShoppingCartDataTest {
		@Autowired
	    private ShoppingCartData s;
		
		/*
		@Test
		public void contextLoads() {
			Customer c = new Customer();
		    c.setId(4);
			Item[] p = new Item[2];
			Product pro = (new Product(25, "Sandalias", "talla 29", 758, 8)); 
			Product pro2 = (new Product(27, "Tacones", "talla 29", 778, 8));
			Date oo = new Date();
			System.out.print(oo.getDate());
			ShoppingCart sho = new ShoppingCart(oo.getDate()+"-"+oo.getMonth()+"-"+oo.getYear(), c);
			p[0] = (new Item(pro.getId(), 0, 2));
			p[1]=(new Item(pro2.getId(), 0, 4));
			ShoppingCart m = s.insertShoppingCart(sho, p);
			
		}*/
		
	
	}


